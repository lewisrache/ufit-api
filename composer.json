{
    "name": "ufit/api",
    "description": "backend API for workout application",
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "require-dev": {
        "phpunit/phpunit": "^9"
    },
    "scripts": {
        "test": "vendor/bin/phpunit tests"
    }
}
